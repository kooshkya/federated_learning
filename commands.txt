ssh kooshkya@er113.utah.cloudlab.us

kooshkya@node-000:~$ qemu-system-x86_64 \
  -enable-kvm \
  -machine type=pc,accel=kvm \
  -cpu host \
  -m 4096 \
  -smp 4 \
  -drive file=p4-vm.qcow2,format=qcow2,if=virtio \
  -netdev user,id=net0,hostfwd=tcp::2222-:22 \
  -device virtio-net-pci,netdev=net0 \
  -display none \
  -serial mon:stdio


ssh p4@localhost -p 2222

sudo p4run

h1 export PYTHONPATH=/home/p4/starter-code/src && cd src
h2 export PYTHONPATH=/home/p4/starter-code/src && cd src
h3 export PYTHONPATH=/home/p4/starter-code/src && cd src
h1 python3 -u worker/worker.py 1 --config config/config.json 2>&1 > h1.log &
h2 python3 -u worker/worker.py 2 --config config/config.json 2>&1 > h2.log &
h3 python3 -u worker/worker.py 3 --config config/config.json 2>&1 > h3.log &

